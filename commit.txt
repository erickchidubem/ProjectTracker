fix(api): resolve Swagger GET conflict by separating paged and all-project endpoints

- Updated ProjectsController to use distinct routes:
  - GET /api/projects -> returns all projects
  - GET /api/projects/paged -> returns paged projects with query params
- Ensured Swagger no longer reports duplicate GET paths
- Improved endpoint clarity and API usability
